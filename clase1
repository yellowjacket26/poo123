#include <iostream>
using namespace std;

int main() {
    int a;
    float b;
    float c;
    c= 0.7;
    b = 0.4522;
    cin >> a;
    if (a<100)
    {
        cout << a * b << "\n" ;
    }
    else
    {
        cout << (100 * b) + c*(a-100);
    }
    return 0;

}
